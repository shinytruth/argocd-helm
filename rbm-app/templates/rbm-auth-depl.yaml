apiVersion: apps/v1
kind: Deployment
metadata:
  name: rbm-auth-depl
spec:
  selector:
    matchLabels:
      app: rbm-auth-depl
  replicas: {{ .Values.replicaCount }}
  template:
    metadata:
      labels:
        app: rbm-auth-depl
    spec:
      containers:
      - name: rbm-auth-depl
        image: rbmjbae/argrocd-test:4.0
        ports:
        - containerPort: 8080
